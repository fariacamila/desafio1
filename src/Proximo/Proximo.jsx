import video from '../img/video.svg'

import './Proximo.scss'

const Proximo = () => {
  return (
    <section className='Video'>
      <img className='img' src={video} alt="#" />
    </section>
  )
}

export default Proximo